# Loan Default Prediction

A Machine Learning project that predicts whether a borrower is likely to default on a loan based on financial, demographic, and loan-related features. The project covers data exploration, preprocessing, feature selection, model training, evaluation, and performance analysis to build an effective loan default prediction system.
